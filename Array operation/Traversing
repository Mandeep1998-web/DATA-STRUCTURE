#include<stdio.h>
#include<conio.h>
int main(){
int arr[]={23,22,3,44444,1,8};
int i,n;
n=sizeof(arr) / sizeof(arr[0]);
printf("The array elements are :\n");
for (i=0;i<n;i++){
printf("arr[%d] = %d\n",i,arr[i]);
}
}

